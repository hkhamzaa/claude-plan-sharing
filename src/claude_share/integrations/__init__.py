"""Adapters to specific external tools/products (Claude Code, and later
others). Nothing in `domain/` or `application/` imports from here - these
are consumers of the existing services, at the same layer as `cli/`."""
